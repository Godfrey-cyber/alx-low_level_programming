#include "main.h"
#include <stdio.h>
/**
 * _strncat - this function that concatenates two strings except that
 * it will use at most n bytes from src.
 * @dest: char to check
 * @src: char to check
 * @n: input value
 * number of bytes from src
 * Description: This will append the src string to the dest string
 * Return: Return a pointer to the resulting string dest
 */
char *_strncat(char *dest, char *src, int n)
{
	int i, j, newstring;

	for (i = 0; dest[i] != '\0'; i++)
	{
	}
	for (newstring = 0; src[newstring] != 0; newstring++)
	{
	}
	for (j = 0; j < n && b < newstring; j++, i++)
	{
		dest[a] = src[b];
	}
	return (dest);
}

