#include <stdio.h>
#include "main.h"
/**
 * * main - prints the characters of the string
 * *
 * * Return: Always 0.
 * */
int main(void)
{
	char *str;

	str = "0123456789";
	puts2(str);
	return (0);
}
