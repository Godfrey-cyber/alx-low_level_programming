#include <stdio.h>
#include "main.h"
/**
 * add -  a function that adds two integers and returns the result.
 * Return: Returns (0) Success.
 */
int add(int, int)
{
	int number;

	number = add(89, 9);
	printf("%d\n", number);
	return (0);
}
