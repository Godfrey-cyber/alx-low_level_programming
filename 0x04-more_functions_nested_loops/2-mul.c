#include <stdio.h>
#include "main.h"
/**
 * mult - a function that multiplies two numbers
 * @a - an integer
 * @b - an integer
 * Return: Returns 0
 */
int mul(int a, int b)
{
	int x;
	x =  a * b;
return (x);
}
