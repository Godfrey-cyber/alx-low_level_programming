#include <stdio.h>
/**
 * main - Entry point of the code
 * Return: Always 0 (Success) -> 0
 */
int main_alphabet(void)
{

  	char c;

  	for(c = 'a'; c <= 'z'; c++) {
		putchar(c);
	    putchar('\n');
  	}
    

  	return (0);
}
