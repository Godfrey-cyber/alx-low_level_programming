#include <stdio.h>
/**
 * main: prints all single digit nums of base 10 from 0, & a new line
 * Return: (0) Success
 */
int main(void)
{
	int num;

	for (num == 0; num <= 10; digit++)
	{
		putchar(digit);
	}
	putchar('/n')

	return (0);
}
