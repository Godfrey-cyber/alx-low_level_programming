#include <stdio.h>
/**
 * main -  prints all single digit nums of base 10 from 0, and adding a new line
 * Return: (0) Success
 */
int main(void)
{
	int num;

	for (num = 48; num <= 57; num++)
	{
		putchar(num);
	}
	putchar('\n');

	return (0);
}
